project architect

GitHub → Jenkins → Docker → Registry → Kubernetes → Monitoring

to copy files from docker to pwd

docker cp container_name:/var/jenkins_home/config.xml ./config.xml


jenkins username - arzun password -admin
